# Backend Configuration
[phases.setup]
nixPkgs = ["python311", "ffmpeg", "pango", "harfbuzz", "fontconfig"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"

